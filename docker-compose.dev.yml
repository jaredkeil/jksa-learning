version: "3"
services:
  db:
    volumes:
      - app-db-data:/var/lib/postgresql/data/pgdata

  web:
    volumes:
      - ./backend/app:/app
      - ./backend/assets:/assets

volumes:
  app-db-data:
