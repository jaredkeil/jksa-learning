version: "3"
services:
  db:
    volumes:
      - app-db-data:/var/lib/postgresql/data/pgdata

  web:
#    volumes:
#      - ./backend/app:/app
#      - ./backend/assets:/assets
    build:
      args:
        - POETRY_WITH_DEV=--with-dev

volumes:
  app-db-data:
